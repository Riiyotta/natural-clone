{
  "id": "conversion.open-roles",
  "category": "conversion",
  "name": "Open roles board",
  "purpose": "The /careers job board: a heading carrying a live-derived role count, beside departments each listing their open roles.",
  "content": {
    "additionalProperties": false,
    "properties": {
      "heading": { "type": "string", "maxWords": 3 },
      "departments": {
        "type": "array", "minItems": 1, "maxItems": 10,
        "items": {
          "additionalProperties": false,
          "properties": {
            "department": { "type": "string", "maxWords": 4 },
            "roles": {
              "type": "array", "minItems": 1, "maxItems": 20,
              "items": { "additionalProperties": false, "properties": { "title": { "type": "string", "maxWords": 8 }, "subtitle": { "type": "string", "maxWords": 6 }, "href": { "type": "string" } }, "required": ["title", "href"] }
            }
          },
          "required": ["department", "roles"]
        }
      }
    },
    "required": ["heading", "departments"]
  },
  "constraints": { "onePerPage": true, "maxPerPage": 1, "mustNotBeFirst": true, "mustNotBeLast": true },
  "motion": { "allowed": ["none"], "reducedMotionFallback": "none" },
  "countRule": "The number rendered beside the heading is DERIVED by summing every department's roles — it is never authored. Authoring it invites the manifest-count drift class of bug.",
  "responsive": { "mobile": { "layout": "stacked" }, "desktop": { "layout": "580px heading column + a flexible roles column" } },
  "background": "surface.hover",
  "measuredFrom": "src/pages/Careers.jsx:319-348, src/pages/Careers.jsx:147"
}
